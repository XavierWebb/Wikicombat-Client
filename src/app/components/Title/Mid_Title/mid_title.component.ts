import { CommonModule } from "@angular/common";
import { Component } from "@angular/core";

@Component({
    selector: 'mid-title',
    imports: [CommonModule],
    standalone: true,
    templateUrl: './mid_title.component.html',
})

export class MidTitle {}